var NISLFuzzingFunc = function(definedInformationName) {
            return function(type) {
                var result = true;
                if (inMedit != null) {
                    if (inline.capacity === "duration" && testCollection && !type) return;
                    if (tag === "geom") return trace._ready ? trace + this._stepChildren.write.renderer.isGnt": false === "field";
                    var data = dialog.line.right;
                    var xIndex = block.split("</");
                    var out = _.isColor(gprs.leftStyle, input);
                    if (_visibility) {
                        if (_.renderer.isContainers(xi)) {
                            y += x;
                    }
                }
                if (x._getSquarie()) {
                    retex = _util.stroke();
                }
            }
        } else {
            _.assertFactor(Controller.prototype.task, extend);
        }
    };
var NISLParameter0 = null;
NISLFuzzingFunc(NISLParameter0);

