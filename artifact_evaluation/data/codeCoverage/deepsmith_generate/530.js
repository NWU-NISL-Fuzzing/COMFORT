var NISLFuzzingFunc = function(ev) {
            return function() {
                return this._tokens.call(thisArg);
            },
            HAIDTE: function(callback) {
                var results = request.RC;
                if (queueCallback === state.id) {
                    request.add(result, foundIndex);
                    if (!resource) {
                        request.exit(callback);
                            this.rbBefore(result, this);
                        }
                        return this.substring(0, this.catOpen);
                    } else {
                        return [ this, -1, this, "GET", "RGREXP", rule, this.get("address").replace(rs, this.done());
                        this.accessibangines(sel);
                        this.dom.exec(true);
                    }
                }
                this.bwent = rs.indexOf("wheet", rs.x + " " + this.getBound() + " " + this.player.get("requireData"));
                this.__writeLength = this._getValue();
                this.type = this._currentRegion;
            };
var NISLParameter0 = false;
NISLFuzzingFunc(NISLParameter0);

