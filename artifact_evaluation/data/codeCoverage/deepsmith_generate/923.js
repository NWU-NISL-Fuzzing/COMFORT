var NISLFuzzingFunc = function(n) {
            return n.call(t, n[0], n[0]);
        };
var NISLParameter0 = [null, false, function(Encoding) {
            var count = base.spacing;
            var path = getController.getPath();
            if (!this.borderStream && this.isPoint === "on" || this.request) {
                return this.tracker;
            },
            list: {
                is: "grunt"
            };
            if (this._children[iNode.initialize()] > this._transnations[i].contains.sort(function() {
                if (item.renderer) {
                    var str = result.timelineController.getBackgrounds(item);
                    this.__expect();
                    this._end = this._start([ this._drawIndex, this.keyStyleChars);
                    this._toUrlText();
                }
            }
            return this;
        }, [undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined, undefined], [-80688539, 1754, -2641], 4823809];
NISLFuzzingFunc(NISLParameter0);

