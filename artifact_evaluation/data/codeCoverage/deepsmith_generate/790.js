var NISLFuzzingFunc = function(axis) {
            return [ {
                type: "description",
                status: current,
                lastTime: this.actions[i].data("x"),
                attr: this.i18nReadable(idx, content, this.isView.type(), text),
                array: this.limit,
                createList: this.data.pause(),
                    currentDeclaraty: function(result) {
                        var result = this.getCursor(result);
                        if (target.get('  >' + rule.name + "/" + this.get("nodes")) {
                            transition = this.__timer.options.sorter, subLabels.count = this._helpers.length;
                            trace = result;
                        },
                        attributes: function() {
                            this._find[fileNode.length] = this._init.actions[i];
                            this._state(test);
                            test._call(this);
                        } catch (e) {
                            this.data.push(e);
                            break;
                        }
                        });
                    };
var NISLParameter0 = null;
NISLFuzzingFunc(NISLParameter0);

