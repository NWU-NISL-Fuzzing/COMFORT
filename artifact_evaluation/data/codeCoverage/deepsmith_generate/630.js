var NISLFuzzingFunc = function(options) {
            if (options.request.query(options, options) || options.isNumedinal) {
                this.responder.ctrl(_instance.series[options._children[index]);
                this._attribute.get("controller");
                this._instance.add(options);
                if (this._renderer) {
                    this._setHeader(options);
                }
                for (i = 0; i < list.length; i++) {
                    action = this._resolve(options);
                    if (options.child.state !== undefined) {
                        this._keyObjectProps_ = args;
                    }
                } else {
                    if (this.__state.type) {
                        this._properties[name].binding = options.shared;
                    }
                    this._projective = options.activeBy ? props.substrings[key] : true;
                }
            }
            this._func = options && this.length;
        };
var NISLParameter0 = true;
NISLFuzzingFunc(NISLParameter0);

