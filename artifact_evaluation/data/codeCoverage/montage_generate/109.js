var v0, v1;
var TA = undefined[++v0][Date[Infinity]][Date], Date = 13;
for ([v0, ...v1] of [[
            1,
            2,
            3
        ]]) {
    assert.sameValue(v0, 1);
    assert.sameValue(v1.length, 2);
    assert.sameValue(v1[0], 2);
    assert.sameValue(v1[1], 3);
    Date += 1;
}
assert.sameValue(v0, 1);