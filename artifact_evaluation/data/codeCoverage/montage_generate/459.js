if (Date.prototype.hasOwnProperty('getFullYear') !== true) {
    new Int32Array(23, 'x');
    Function && decodeURIComponent(1, 123, 8).x('function', NaN);
    return !new Date(Int8Array < 'a').slice;
}