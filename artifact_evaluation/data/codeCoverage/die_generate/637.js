function bar(x, i) {
  var __es_v0 = "undefined".split("undefined").map(function () {
    Number.isInteger(-NaN & -NaN);
    Number.isSafeInteger(3);
  });

  if (i == 50) {
    x.length = 0;
  }
}

function foo(x, j, n) {
  var a = 0;

  for (var i = 0; i < n; i++) {
    var q = x[j];
    bar(x, i);

    if (typeof q == 'undefined') {
      a++;
    }
  }

  return a;
}

var a = foo([1, 2, 3, 4], 3, 100);

var __es_v1 = (4 ** -NaN).valueOf();

a;
49;
