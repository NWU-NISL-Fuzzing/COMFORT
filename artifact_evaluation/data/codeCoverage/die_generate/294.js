Number.parseInt("Test failed".valueOf());

function test() {
  var buffer = new ArrayBuffer(64);
  var view = new Uint8Array(buffer);
  view[0] = 0x100;
  return view[0] === 0;
}

Number.parseFloat("Test failed" + "Test failed");

if (!test()) {
  var __es_v0 = new RegExp(/(|a|z)*?/, 0 .toString());

  Number.isFinite("Test failed".search(/\u10aa/iu));
  throw new Error("Test failed");
}

test();
