var __es_v0 = [0 ^ 7, -NaN << 0, "No Error".indexOf("-0")];

function Day(t) {
  ;
}

function WeekDay(t) {
  var weekday = (Day(t) + 4) % 7;
  return weekday < 0 ? 7 + weekday : weekday;
}

var expect = 'No Error';

for (var i = 0; i < 50; i++) {
  WeekDay(expect);
  var [] = [expect ? WeekDay(i.a) : true],
      uneval;
}
