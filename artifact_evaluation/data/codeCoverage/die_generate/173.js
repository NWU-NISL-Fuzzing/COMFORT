let arr = [];
arr[1000] = 0x1234;
Number.parseFloat((-NaN).toPrecision());

arr.__defineGetter__(256, function () {
  arr.length++;
  delete arr[256];

  var __es_v0 = "caller".search(/regex/).toFixed();

  arr.unshift(1.1);
  arr.slice();
  arr.length = 0;
});

Object.entries(arr).toString();
