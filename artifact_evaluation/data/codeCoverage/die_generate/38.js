console.log("This test checks that declarations in an if block can be seen outside it." + "This test checks that declarations in an if block can be seen outside it." + String.prototype.padStart.call("This test checks that declarations in an if block can be seen outside it.", -NaN));

if (0) {
  var b;
}

var a = b;
