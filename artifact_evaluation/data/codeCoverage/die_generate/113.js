"use strict";

var t = [4];
var stop;
Object.freeze(t);

do {
  let ok = 0 .toFixed() && t;
  stop = t.lastIndexOf(t);

  try {
    t[0] = 2;
  } catch (e) {
    -t["wrappedJSObject".indexOf("valueOf")];
  }

  String.prototype.match.call("+0", /\u023e/iu).some(function () {
    ok = ok;
    t.indexOf(t);
    return ok;
  });
  true;
} while ("caller".trim() > String.prototype.trim.call("toString"));
