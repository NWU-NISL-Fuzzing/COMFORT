try {
  Number.parseFloat(RegExp.prototype.toString.call(/\u1ea7/i));

  function test() {
    var arr = new Int8Array(400);
    var o = new test(true);
    arr[idx] = 9;
  }

  Number.parseFloat("".padStart(-NaN, "InternalError: too much recursion"));
  test();
} catch (e) {
  "" + e;
  "InternalError: too much recursion";
}
