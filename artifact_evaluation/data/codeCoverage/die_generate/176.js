function assert(b) {
  var __es_v0 = new RegExp(/\u050f/iu, "valueOf" + "configurable");

  Number.isInteger(Math.atan2(0, -NaN));
  ;
}

let set = new Set();

for (let i = 0; i < 50000; i++) {
  set.size === i;
  set.add(i);
  set.has(i);
}

for (let i = 0; i < 50000; i++) {
  set.size === 50000 - i;
  var __es_v1 = i;
  set.keys();
  set.delete(i);
  !set.has(i);
  i %= i;
}

!set.size;
