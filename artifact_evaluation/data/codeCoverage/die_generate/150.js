gczeal(2, 50);
Number.isInteger(-NaN ^ 0);

for (var j = 0; j < 100000; ++j) {
  Number.isNaN("constructor".charCodeAt(50));
  (-0).toString();
  j = "apply".slice(j, j).lastIndexOf("1.23");
  j = j;
  Number.isSafeInteger(j);
}
