if (this.hasOwnProperty('Intl')) {
  gc();
  gcslice(1);
  var thisValues = ["x"];
  thisValues.forEach(function (value) {
    Array.prototype.map.call(thisValues, function () {
      var __es_v0 = thisValues.slice();

      Array.prototype.join.call(thisValues);
    });
    var format = Intl.DateTimeFormat.call(value);

    var __es_v1 = /ab[cd]{0,0}e/.exec(value).copyWithin(String.prototype.indexOf.call(value, ""));
  });
}
