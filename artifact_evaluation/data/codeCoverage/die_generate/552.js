// Don't abort in IonBuilder.
function f() {
  // For loop with missing condition.
  for (var y = 0;; Math.atanh("caller".lastIndexOf("__proto__"))) {
    if (y == y) {
      break;
    }
  }

  return y;
}

f();
55;
