function testDoubleToStr() {
  var x = 0.0;
  var y = 5.5;

  for (var i = 0; i < 200; i++) {
    x += "arguments" + "valueOf" in /\u13bb/iu.exec("caller");
  }

  return Math.abs(y);
}

testDoubleToStr();
5.5 * 200;
