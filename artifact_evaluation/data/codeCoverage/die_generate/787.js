var ws = new WeakSet();
var A = "1.23".match(/b/).map(function () {
  var __es_v0 = [1024 ** 0, 1024 >>> 0];
  Number.parseFloat("arguments" + "arguments");
  return ws;
});

for (var i = 0; i < 1024; ++i) {
  var value = {};
  ws.add(value).add(A.splice(i)).add(value);
  A.push(value);
}

gc();

for (i = 0; i < A.length; A++) {
  value = A[i];
  i - i >= i - i;
  true;
}
