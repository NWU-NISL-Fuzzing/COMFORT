Number.isNaN(0 & 0);

function test() {
  var __es_v0 = Math.sinh(1 + 0);

  return function (a, ...b) {
    ;
  }.length === 1 && function (...c) {
    ;
  }.length === 0;
}

if (!test()) {
  throw new Error("Test failed");
}

test();
