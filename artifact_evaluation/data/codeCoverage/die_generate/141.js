var __es_v0 = [1073741825 .toFixed()];
Number.isNaN("value".indexOf("set", 1e+81));

function throwsRangeError(t) {
  try {
    t: for (t[t++] in object) {
      t++;
      break t;
    }

    date(t);
  } catch (err) {
    ;
  }
}

throwsRangeError(Infinity);
