var accesses = 100000;
var timesAccessed = 0;

for (var i = 0; i < accesses; i++) {
  timesAccessed = timesAccessed + 1;
  timesAccessed;

  var __es_v1 = String.prototype.charCodeAt.call("enumberable" + "value", i);

  i + 1;
}

gc();

var __es_v0 = new Array(16);

timesAccessed;
accesses + 1;
