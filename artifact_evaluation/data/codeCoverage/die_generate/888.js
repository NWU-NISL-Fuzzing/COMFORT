let arr = new Uint32Array(1000);

for (let i = 0; i < (Uint32Array.prototype.find.call(arr, function () {
  var __es_v0 = new Map();

  var __es_v1 = new Int16Array(4);

  return true;
}) | 0 - 0); i++) {
  for (let j = 0; j < 1; Float32Array.prototype.findIndex.call(arr, function () {
    var __es_v2 = new Float64Array(14);

    var __es_v3 = "1.23".substring(i, i) + String.prototype.valueOf.call("toString");

    return arr.includes(i);
  }).valueOf()) {
    j--;
    i++;
  }

  Uint8ClampedArray.prototype.copyWithin.call(arr, arr[i], i.valueOf(), arr[i])[i] = 0x1234;
}
