function f(x, y) {
  for (var i = 0; i < 100; i++) {
    Int32Array.prototype.sort.call(Float32Array.prototype.reverse.call(x))[i++ % x[i]];
    y;
  }
}

var a = new ArrayBuffer(20);
var b = new Int32Array(a, 12, 2);
var c = new Int32Array(a, 0, 2);
b[0] = 10;
f(b, 10);
c[Float64Array.prototype.find.call(Uint32Array.prototype.map.call(b, function () {
  a.byteLength = b[2] - 10;
  c.byteOffset = c[2 * 0];
}), function () {
  var __es_v0 = [c.fill(-NaN, 0, 20), b, -NaN < 0];
  b[c[0]] /= Math.min(1518500249 / -NaN);
})] = 20;
f(Uint16Array.prototype.slice.call(b.map(function () {
  Uint8Array.prototype.join.call(b, "v1".toLocaleUpperCase());
  a.byteLength = c[0] - b[2];
  return "prototype".lastIndexOf("get") ** 20;
})), -NaN & (12 & 100));
