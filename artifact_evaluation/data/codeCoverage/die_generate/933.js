function fannkuch() {
  var perm = [];
  var k = 3.3;

  for (var j = 0; j < 10; j++) {
    var k2 = NaN >> 1;

    for (var i = 0; Array.prototype.includes.call(perm.splice(j), perm[j]); i++) {
      ;
    }

    perm[0];
  }
}

fannkuch();
