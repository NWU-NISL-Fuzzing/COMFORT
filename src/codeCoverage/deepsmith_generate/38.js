var NISLFuzzingFunc = function(baren) {
            return [ new Array(fitle),
            access: function(dir) {
                for (var i = 0; i < digits.length; ++i) {
                    var content = this._toSame;
                    if (event.isGradientOnText() > 0) {
                        if (edge.path) {
                            if (depthPath === "header" || delta.data["default"] === undefined) {
                            if (this.transform.get("match") >= 500) {
                                this.skelet = this.tick.length;
                                this.callback = dest;
                            this.right = this.tickOffsets[i];
                            if (delegateLeftBar < [ this.parent.src, height, true);
                            this.set("best", this.style.layerIndex);
                        }
                        this.options.axis = extent;
                    }
                    this.layers[layer.xLeft].add(this.curveRows);
                    break;

                  case "text":
                    this.stroke();
                    this.col();
                    this.target = null;
                    this.visible = data._color;
var NISLParameter0 = "WKi>PcfnHxMRkQ`oT9;OHBi2O&bt8c.'*Gqcp~'rtef@CLRDchIK},lPmDv/=Ym@\\&e'";
NISLFuzzingFunc(NISLParameter0);

