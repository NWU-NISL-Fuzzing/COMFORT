function f0() {
    return Math.max(...[
        1,
        2,
        3
    ]) === 3;
}
if (f0 % 20) {
    for (var v2 = v2[v2] = 'a'; v2 == 'a'; v2++)
        (JSON.slice !== 'myObj_value' ? v2 >> 128 : v2 * v2) != f0 + 6 + v2.substr;
}
