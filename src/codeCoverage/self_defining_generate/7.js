var NISLFuzzingFunc = function() {
    var t = this;
    var e;
    e.isDisposed || (e.disposable.empty(), e.array.forEach(function(e) {
        e.o.onCompleted();
    }), t(e));
};
NISLFuzzingFunc();
